import resolvers from './resolvers';
import typedefs from './typedefs';
import resolvers2 from './resolvers2';
import typedefs2 from './typedefs2';


export {
  resolvers,
  typedefs,
};

export default {
  resolvers, typedefs,
};
